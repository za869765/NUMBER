{
  "name": "hiv-auth",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply hiv-auth-db --remote",
    "migrate:local": "wrangler d1 migrations apply hiv-auth-db --local",
    "init-db": "wrangler d1 migrations apply hiv-auth-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.88.0"
  }
}
